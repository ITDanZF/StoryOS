## 动机

说明为什么需要这次改动。

## 实现

简要描述做了什么、动到哪些边界（Agent 引擎 / StoryOS 业务 / renderer / 契约 / 存储）。

## 验证

- [ ] 已运行与改动范围匹配的检查（注明命令）
- [ ] 如改动 UI，已在实际界面过一遍相关路径
- [ ] 未引入未约定的备用字段、静默兼容或调试残留

## 风险

数据迁移、权限、原生模块 ABI、或行为变化请写在这里。没有则写「无」。
