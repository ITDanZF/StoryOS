# 隐私说明

StoryOS 是本地优先的桌面应用。维护者不运营默认云端账号，也不把你的稿件上传到 StoryOS 自己的服务器。

本说明描述当前 `main` 分支的实际行为。若实现变化，应同步更新本文。

## 默认保存在本机的数据

首次使用需要创建实例。实例目录由你指定，建议路径为 `%USERPROFILE%\.mini-agent`，也可以换成其他空目录。

实例目录内通常包括：

- `config.json`：对话模型、可选 embedding 配置，以及明文 API Key
- `app.sqlite`：应用级登记数据
- 项目与书籍目录中的 SQLite 数据库：书籍、章节修订、草稿、会话、运行记录等

实例列表保存在 Electron 用户数据目录中的 `instances.json`（Windows 上通常位于 `%APPDATA%\storyos\`）。该文件记录实例名称、路径和最近打开时间，不保存稿件正文。

本版本不会自动接管或删除旧版 `%USERPROFILE%\.mini-agent` 中的数据。

## API Key

对话和 embedding 的 API Key 由你自行向模型服务商申请，并写入对应实例的 `config.json`。

- StoryOS 不提供内置平台账号。
- 保存时使用 Unix 文件模式 `0600`。在 Windows NTFS 上这不等于加密，也不等于完整的 ACL 锁定；能读取该文件的本机账户就能看到密钥。
- 密钥不会被发送到 StoryOS 维护者，只会在你发起的模型请求中作为服务商要求的认证信息使用。

## 何时会离开本机

只有在你主动使用 AI 功能（对话、章节生成、embedding 测试或后续检索）时，完成该请求所需的内容才会发送到**你配置的**模型服务地址，例如 OpenAI、DeepSeek、通义千问或其他 OpenAI 兼容接口。

发送范围取决于当次任务，可能包括提示词、当前章节摘录、工具读取的正文片段和你输入的指令。StoryOS 无法控制这些服务商如何存储或训练数据，请在使用前阅读对方的隐私政策。

未启用 AI、也未配置可用密钥时，编辑、阅读、导入导出和备份都可以只在本地完成。

## 日志

StoryOS 当前没有独立的遥测或崩溃上报服务，不会把使用情况发回维护者。

开发或打包运行时，错误可能输出到控制台或操作系统崩溃记录。这些记录可能包含路径、实例标识或错误消息；不要假设其中一定不含用户内容。会话正文的权威存储仍是实例内的 SQLite，而不是远程日志。

## 第三方责任边界

| 主体 | 职责 |
| --- | --- |
| StoryOS | 在本机保存数据、按你的配置发起请求 |
| 你配置的模型服务商 | 接收请求后的处理、保留与隐私政策 |
| 操作系统与本机账户 | 磁盘加密、备份、其他软件对实例目录的访问 |

## 删除数据

1. 在实例面板把实例从列表移除：只取消登记，**不会删除**实例目录里的文件。
2. 要彻底删除稿件、会话和密钥：关闭应用后，删除对应实例目录，并按需删除 `%APPDATA%\storyos\instances.json`。
3. 已发送给模型服务商的内容，需要按该服务商的账户与数据删除流程处理；StoryOS 无法代为撤回。

定期备份请复制实例目录，或从书架导出 StoryOS 完整备份（`.storyos-book`）。外部稿件格式（DOCX、Markdown、TXT 等）只交换当前正文，不含修订历史。
